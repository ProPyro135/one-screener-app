# Runtime deps for the hosted read-only dashboard (Streamlit Community Cloud,
# installed with uv). Left unpinned on purpose: the host runs a very recent
# Python (3.14+), and exact pins to older versions have no matching wheels there
# — uv would build numpy/pandas/duckdb from source and time out. The dashboard
# uses only stable APIs, so the current releases are safe. Pin here later if a
# future major release breaks something.
streamlit
plotly
pandas
duckdb
numpy
