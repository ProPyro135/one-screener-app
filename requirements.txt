# Runtime dependencies for the hosted read-only dashboard (Streamlit Community
# Cloud installs from this file). These are the reader's needs only — the
# ingestion stack (yfinance, requests, openpyxl) is not imported by app/dashboard.py.
# Versions match pyproject.toml so the host matches local.
streamlit==1.62.0
plotly==6.9.0
pandas==2.3.3
duckdb==1.5.5
numpy==2.2.6
